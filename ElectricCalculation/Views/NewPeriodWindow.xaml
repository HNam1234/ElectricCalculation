<Window x:Class="ElectricCalculation.Views.NewPeriodWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:behaviors="clr-namespace:ElectricCalculation.Behaviors"
        Title="Làm tháng mới"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        behaviors:DialogCloser.DialogResult="{Binding DialogResult}">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Tháng:" VerticalAlignment="Center" Margin="0,0,8,0"/>
        <TextBox Grid.Row="0" Grid.Column="1" Width="80" Text="{Binding Month, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Năm:" VerticalAlignment="Center" Margin="0,8,8,0"/>
        <TextBox Grid.Row="1" Grid.Column="1" Width="80" Margin="0,8,0,0"
                 Text="{Binding Year, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Tháng cũ:" VerticalAlignment="Center" Margin="0,8,8,0"/>
        <ComboBox Grid.Row="2" Grid.Column="1"
                  Width="420"
                  Margin="0,8,0,0"
                  ItemsSource="{Binding ReferenceDatasets}"
                  SelectedItem="{Binding SelectedReferenceDataset, Mode=TwoWay}"
                  DisplayMemberPath="DisplayName" />

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Margin="0,12,0,0">
            <CheckBox Content="Chuyển chỉ số mới -> chỉ số cũ" Margin="0,6,0,0" IsChecked="{Binding MoveCurrentToPrevious, Mode=TwoWay}" />
            <CheckBox Content="Xóa chỉ số mới (để trống)" Margin="0,6,0,0" IsChecked="{Binding ResetCurrentToZero, Mode=TwoWay}" />

            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="#C00000"
                       Margin="0,10,0,0"
                       TextWrapping="Wrap" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Width="90" Margin="0,0,8,0" Content="OK" IsDefault="True" Command="{Binding OkCommand}"/>
                <Button Width="90" Content="Hủy" IsCancel="True" Command="{Binding CancelCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
